export default defineComponent({
  render () {
    return (
      <div>
        <h1>Page created with .tsx extension</h1>
        <ol>
          <li>Create file with .ts extension</li>
          <li>Export and define the component using `defineComponent` function</li>
          <li>Write the render function inside the `defineComponent` function</li>
          <li>Return with JSX syntax </li>
        </ol>
      </div>
    )
  }
})